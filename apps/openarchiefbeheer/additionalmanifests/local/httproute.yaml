apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: openarchiefbeheer
  namespace: publicground
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: gateway-local
      namespace: envoy-gateway-system
      sectionName: http
  hostnames:
    - "openarchiefbeheer.wigo4it.nl"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - group: ""
          kind: Service
          name: openarchiefbeheer-nginx
          port: 80
          weight: 1
