apiVersion: k8s.keycloak.org/v2alpha1
kind: Keycloak
metadata:
  name: keycloak
spec:
  hostname:
    hostname: keycloak.wigo4it.nl
  unsupported:
    podTemplate:
      spec:
        containers:
          - name: keycloak
            envFrom:
              - secretRef:
                  name: keycloak-realm-secrets
