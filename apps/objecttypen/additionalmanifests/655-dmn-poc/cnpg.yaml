apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: objecttypen-pg-cluster
  namespace: publicground
spec:
  instances: 1
  storage:
    size: "1Gi"
    storageClass: managed-csi
  bootstrap:
    initdb:
      database: "objecttypen"
      owner: "objecttypen"
