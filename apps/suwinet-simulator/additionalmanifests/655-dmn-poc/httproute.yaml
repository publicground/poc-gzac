apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: suwinet-simulator
  namespace: publicground
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: main-gateway
      namespace: envoy-gateway-system
      sectionName: https
  hostnames:
    - "gzac.dmn-poc.publicground.nl"
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /ws
      backendRefs:
        - group: ""
          kind: Service
          name: suwinet-simulator
          port: 8080
          weight: 1
